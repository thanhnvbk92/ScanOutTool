<Window x:Class="ScanOutTool.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:ScanOutTool.Helpers.Converters"
        Title="Modern WPF App"
        Height="600"
        Width="1000"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Light"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource MaterialDesignBackground}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource MaterialDesignSelection}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TitleBarCloseButtonStyle" TargetType="Button" BasedOn="{StaticResource TitleBarButtonStyle}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E81123" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#C50F1F" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <local:BoolToWidthConverter x:Key="BoolToWidthConverter" />
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{Binding IsSidebarOpen, Converter={StaticResource BoolToWidthConverter}}" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <DockPanel Background="{DynamicResource MaterialDesignBackground}" Grid.Column="0">
            <StackPanel>
                <Button Command="{Binding ToggleSidebarCommand}" Background="Transparent" BorderThickness="0" Margin="10" HorizontalAlignment="Left">
                    <materialDesign:PackIcon Kind="Menu" Width="30" Height="30" Foreground="{DynamicResource MaterialDesignBody}" />
                </Button>

                <ListBox x:Name="PageListBox" SelectedIndex="{Binding SelectedMenuIndex}" 
                         BorderThickness="0"
                         Background="Transparent"
                         Foreground="{DynamicResource MaterialDesignBody}"
                         Margin="0,20,0,0">
                    <ListBoxItem>
                        <StackPanel Orientation="Horizontal" Margin="10">
                            <materialDesign:PackIcon Kind="Home" Width="24" Height="24" Foreground="{DynamicResource MaterialDesignBody}" />
                            <TextBlock Text="Dashboard" Margin="10,0,0,0" Foreground="{DynamicResource MaterialDesignBody}" VerticalAlignment="Center" />
                        </StackPanel>
                    </ListBoxItem>
                    <ListBoxItem>
                        <StackPanel Orientation="Horizontal" Margin="10">
                            <materialDesign:PackIcon Kind="Cog" Width="24" Height="24" Foreground="{DynamicResource MaterialDesignBody}" />
                            <TextBlock Text="Settings" Margin="10,0,0,0" Foreground="{DynamicResource MaterialDesignBody}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </ListBoxItem>
                    <ListBoxItem>
                        <StackPanel Orientation="Horizontal" Margin="10">
                            <materialDesign:PackIcon Kind="InformationOutline" Width="24" Height="24" Foreground="{DynamicResource MaterialDesignBody}" />
                            <TextBlock Text="About" Margin="10,0,0,0" Foreground="{DynamicResource MaterialDesignBody}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </ListBoxItem>
                    <ListBoxItem>
                        <StackPanel Orientation="Horizontal" Margin="10">
                            <materialDesign:PackIcon Kind="Update" Width="24" Height="24" Foreground="{DynamicResource MaterialDesignBody}" />
                            <TextBlock Text="Check Update" Margin="10,0,0,0" Foreground="{DynamicResource MaterialDesignBody}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </ListBoxItem>
                </ListBox>
            </StackPanel>
        </DockPanel>

        <!-- Main Content -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Custom Title Bar -->
            <Grid Grid.Row="0" Background="{DynamicResource MaterialDesignPaper}" Height="40" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Icon Program -->
                <Image Source="pack://application:,,,/Assets/AppIcon.ico"
                   Width="24" Height="24"
                   Margin="10,0,5,0"
                   VerticalAlignment="Center" />

                <!-- Title -->
                <TextBlock Grid.Column="1" Text="{Binding Title}"
               VerticalAlignment="Center"
               FontWeight="Bold"
               FontSize="14"
               Foreground="{DynamicResource MaterialDesignBody}" />

                <!-- Buttons -->
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Width="40" Height="40" Click="MinimizeButton_Click" Style="{StaticResource TitleBarButtonStyle}">
                        <materialDesign:PackIcon Kind="WindowMinimize" Width="20" Height="20" VerticalAlignment="Center" HorizontalAlignment="Center" />
                    </Button>

                    <Button Width="40" Height="40" Click="MaximizeButton_Click" Style="{StaticResource TitleBarButtonStyle}">
                        <materialDesign:PackIcon Kind="WindowRestore" Width="20" Height="20" VerticalAlignment="Center" HorizontalAlignment="Center" />
                    </Button>

                    <Button Width="40" Height="40" Click="CloseButton_Click" Style="{StaticResource TitleBarCloseButtonStyle}">
                        <materialDesign:PackIcon Kind="Close" Width="20" Height="20" VerticalAlignment="Center" HorizontalAlignment="Center" />
                    </Button>
                </StackPanel>
            </Grid>

            <!-- Theme Toggle -->
            <Border Grid.Row="1" BorderBrush="{DynamicResource MaterialDesignBody}" BorderThickness="0,0,0,1">
                <DockPanel  Background="{DynamicResource MaterialDesignPaper}" Height="50">
                    <TextBlock Text="{Binding PageName}" VerticalAlignment="Bottom" Margin="10" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource MaterialDesignBody}" />
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="10">
                        <materialDesign:PackIcon Kind="WeatherSunny" Width="20" Height="20" Foreground="{DynamicResource MaterialDesignBody}" />
                        <ToggleButton IsChecked="{Binding IsDarkTheme, Mode=TwoWay}" 
                      Width="50" Height="30" Margin="5,0,5,0"
                      Style="{StaticResource MaterialDesignSwitchToggleButton}" />
                        <materialDesign:PackIcon Kind="WeatherNight" Width="20" Height="20" Foreground="{DynamicResource MaterialDesignBody}" />
                    </StackPanel>
                </DockPanel>
            </Border>
            

            <!-- Main Frame -->
            <Frame x:Name="MainFrame" Grid.Row="2" Background="{DynamicResource MaterialDesignPaper}" NavigationUIVisibility="Hidden" />
        </Grid>
    </Grid>
</Window>
