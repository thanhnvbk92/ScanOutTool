<Page x:Class="ScanOutTool.Views.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:controls="clr-namespace:ScanOutTool.Views.Controls"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      Title="Settings">
    <Page.Resources>
        <Style TargetType="GroupBox"  BasedOn="{StaticResource MaterialDesignGroupBox}">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="BorderBrush" Value="{DynamicResource MaterialDesignDivider}"/>
            <Setter Property="BorderThickness" Value="0.5"/>
            <Setter Property="Background" Value="{DynamicResource MaterialDesignBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
        </Style>
    </Page.Resources>
    <Grid Margin="5" IsEnabled="{Binding CanEdit}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBox Text="{Binding ServerIP}" HorizontalAlignment="Left" VerticalAlignment="Center" Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="Server IP" Foreground="{DynamicResource MaterialDesignBody}" Width="100"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <CheckBox Content="Robot Mode" IsChecked="{Binding IsRobotMode}" HorizontalAlignment="Right" Foreground="{DynamicResource MaterialDesignBody}" Visibility="Hidden"/>
            <CheckBox Content="BlockRFMode" IsChecked="{Binding IsBlockRFMode}" HorizontalAlignment="Right" Margin="10,0,0,0" Foreground="{DynamicResource MaterialDesignBody}"/>
            <CheckBox Content="Work Oder Mode" IsChecked="{Binding IsWOMode}" HorizontalAlignment="Right" Foreground="{DynamicResource MaterialDesignBody}"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Visibility="{Binding IsRobotMode,Converter={StaticResource BoolToVisibilityConverter}}">
            <TextBox Text="{Binding PLCIP}" HorizontalAlignment="Left" VerticalAlignment="Center" Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="PLC IP" Foreground="{DynamicResource MaterialDesignBody}" Width="100" Margin="0,0,10,0"/>
            <TextBox Text="{Binding PLCPort}" HorizontalAlignment="Left" VerticalAlignment="Center" Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="PLC Port" Foreground="{DynamicResource MaterialDesignBody}" Width="100" Margin="0,0,10,0"/>
        </StackPanel>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>
            <TextBox Text="{Binding ShopFloorLogPath}" HorizontalContentAlignment="Left" VerticalAlignment="Center" Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="ShopFloor Log Folder" Foreground="{DynamicResource MaterialDesignBody}"/>
            <Button Content="..." Command="{Binding LogBrowserCommand}" Width="80" Grid.Column="1" HorizontalAlignment="Right"/>
        </Grid>
       
        <Grid Grid.Row="3" Margin="0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <GroupBox Header="Scanner Port">
                <controls:SerialPortSettingView DataContext="{Binding ScannerPortSettingVM}"/>
            </GroupBox>
            <GroupBox Header="Shop Floor Port" Grid.Column="1">
                <controls:SerialPortSettingView DataContext="{Binding ShopFloorPortSettingVM}"/>
            </GroupBox>
        </Grid>
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Command="{Binding SaveCommand}" Width="80"/>
        </StackPanel>
    </Grid>
</Page>
