<Page x:Class="ScanOutTool.Views.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:controls="clr-namespace:ScanOutTool.Views.Controls"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      Title="Settings">
    <Page.Resources>
        <Style TargetType="GroupBox"  BasedOn="{StaticResource MaterialDesignGroupBox}">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="BorderBrush" Value="{DynamicResource MaterialDesignDivider}"/>
            <Setter Property="BorderThickness" Value="0.5"/>
            <Setter Property="Background" Value="{DynamicResource MaterialDesignBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
        </Style>
    </Page.Resources>
    <Grid Margin="5" IsEnabled="{Binding CanEdit}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBox Text="{Binding ServerIP}" HorizontalAlignment="Left" VerticalAlignment="Center" Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="Server IP" Foreground="{DynamicResource MaterialDesignBody}" Width="100"/>
        <CheckBox Content="Robot Mode" IsChecked="{Binding IsRobotMode}" HorizontalAlignment="Right" Foreground="{DynamicResource MaterialDesignBody}"/>
        <Grid Grid.Row="1" Margin="0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <GroupBox Header="Scanner Port">
                <controls:SerialPortSettingView DataContext="{Binding ScannerPortSettingVM}"/>
            </GroupBox>
            <GroupBox Header="Shop Floor Port" Grid.Column="1">
                <controls:SerialPortSettingView DataContext="{Binding ShopFloorPortSettingVM}"/>
            </GroupBox>
        </Grid>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Command="{Binding SaveCommand}" Width="80"/>
        </StackPanel>
    </Grid>
</Page>
